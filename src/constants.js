/** Local midnight — April 2, 2026 (change to `new Date(0)` briefly to preview the full experience). */
export const UNLOCK_DATE = new Date(2026, 3, 2, 0, 0, 0, 0);

/** Calm romantic ambient (Mixkit — replace with your own file in /public if preferred) */
export const ROMANTIC_AUDIO_URL =
  "https://assets.mixkit.co/music/preview/mixkit-slow-ambient-1040.mp3";
